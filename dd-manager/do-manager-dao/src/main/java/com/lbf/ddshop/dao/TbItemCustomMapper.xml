<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lbf.ddshop.dao.TbItemCustomMapper" >



  <sql id="Base_Column_List" >
    i.id, i.title, i.sell_point as sellPoint, i.price, i.num, i.barcode, i.image, i.cid, i.status, i.created, i.updated,c.name as catName,
     CASE i.status
		WHEN 1 THEN "正常"
		WHEN 2 THEN "下架"
		WHEN 3 THEN "删除"
		ELSE "未知" END as statusName
  </sql>

  <select id="countItems"  resultType="int">
    select count(1) from tb_item
  </select>

    <select id="listItemsByPage" parameterType="com.lbf.ddshop.common.dto.Page" resultType="com.lbf.ddshop.pojo.vo.TbItemCustom">

        SELECT
          <include refid="Base_Column_List"/>
        from tb_item i
         LEFT JOIN tb_item_cat c ON i.cid=c.id
        WHERE i.status!=3
         ORDER BY i.id DESC
        limit #{offset},#{rows}

    </select>

</mapper>